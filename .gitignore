# 运行数据（zurl sqlite/redis、mihomo 缓存）
data/

# 本地环境变量（含内部令牌，参考 .env.example）
.env

# mihomo 实际配置（含密钥，参考 config/mihomo/config.example.yaml）
config/mihomo/config.yaml
# mihomo 转换生成的最新配置（运行时产物）
config/mihomo/latest.yaml

# metacubexd 面板镜像包与解压产物（运行时产物）
metacubexd-image.tar.gz
metacubexd/

# 依赖与构建产物
node_modules/
dist/
.output/
.nuxt/
.nitro/
**/__pycache__/
*.pyc

# 系统文件
.DS_Store
Thumbs.db
